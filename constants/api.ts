/**
 * Base URL for the Poddy API.
 * Points to the root /api — individual modules append their own prefixes
 * (e.g. "/podcasts/upload", "/auth/login").
 */
export const API_BASE = "http://172.20.10.5:5000/api";